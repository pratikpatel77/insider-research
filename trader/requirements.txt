requests==2.31.0
pandas==2.2.3
numpy==2.1.0
pyotp==2.10.0
# Only the broker you use is needed (setup.bat asks which one):
#   Definedge : nothing more (plain HTTPS)
#   Kotak Neo : pip install kotakneoapi==3.0.6
#   Fyers     : pip install fyers-apiv3==3.1.17
pytest==9.1.1
